package fr.o80.version.domain

import com.intellij.openapi.project.Project
import fr.o80.version.model.VersionSettings

class SaveSettings(project: Project) {
    operator fun invoke(settings: VersionSettings) {

    }
}
